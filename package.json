{
  "name": "svelte-template",
  "type": "module",
  "version": "1.0.0",
  "description": "Svelte template based on esbuild",
  "scripts": {
    "build": "node esbuild",
    "devel": "node esbuild --devel"
  },
  "devDependencies": {
    "@tsconfig/svelte": "^3.0.0",
    "esbuild": "^0.14.36",
    "esbuild-svelte": "^0.7.0",
    "svelte": "^3.47.0",
    "svelte-preprocess": "^4.10.6",
    "typescript": "^4.6.3"
  }
}
